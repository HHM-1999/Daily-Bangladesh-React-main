import React from 'react';
import ReactDOM from 'react-dom/client';
import Emythmakers from './Emythmakers';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
    {/* <React.StrictMode> */}
    <Emythmakers />
    {/* </React.StrictMode> */}
  </>
);